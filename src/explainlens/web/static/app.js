/**
 * ExplainLens Web UI — Minimal Client JavaScript
 *
 * Handles run form submission, status polling, tab switching,
 * and artifact loading. No frameworks — vanilla JS only.
 */

// Empty stub — interactive logic is embedded in templates
// via {% block scripts %}. This file is available for shared
// utility functions that may be needed across pages.
